{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "MongoDbSettings": {
    // for debug localy
    //"ConnectionString": "mongodb://localhost:27017/",
    // for debug localy with Docker container
    //"ConnectionString": "mongodb://localhost:27019/",
    // without debug with Docker container via docker-compose
    "ConnectionString": "mongodb://pcf-commondata-db:27017/", 
    "DatabaseName": "commondataDb"
  },
  "RedisSettings": {
    // for debug localy with Docker container
    //"ConnectionString": "localhost:6379,ssl=False,abortConnect=False"
    // without debug with Docker container via docker-compose
    "ConnectionString": "redis:6379,ssl=False,abortConnect=False"
  },
  "AllowedHosts": "*"
}
